<head>
  <%= stylesheet_link_tag    'venues', media: 'all', 'data-turbolinks-track' => true %>
</head>

<div id = "header" class = "col-xs-12 center">
	<div id="header-background">
		<h1>The best restaurants and bars for big groups brought to you by the people who live there.</h1>
	</div>
</div>

<div id="new-button" class="col-md-12 center">
   <a class="btn btn-warning" href="/venues/new">Add a New Place</a>
</div>

<table class='table table-striped'>
  <thead>
    <tr>
      <th>Name</th>
      <th>City</th>
      <th>Neighborhood</th>
      <th>Cuisine</th>
      <th>Likes</th>
      <th>Reviews</th>
    </tr>
  <tbody>
    <% @venues.each do |venue| %>
      <tr>
        <td><%= link_to venue.name, "/venues/#{venue.id}" %></td>
        <td><%= venue.city %></td>
        <td><%= venue.neighborhood %></td>
        <td><%= venue.cuisine %></td>
        <td>50</td>
        <td>11</td>
      </tr>
    <% end %>
  </tbody>
</table>